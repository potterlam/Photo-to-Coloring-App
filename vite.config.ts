import { defineConfig } from 'vite';

export default defineConfig({
  // 如果您的 GitHub 儲存庫名稱叫做 Photo-to-Coloring-App，這裡請填上 /儲存庫名稱/
  // 若您是掛在自訂網域或是 User Pages (例如 username.github.io)，則設為 '/'
  base: '/Photo-to-Coloring-App/',
});